# Source: https://github.com/onflow/cadence/blob/master/tools/ast-explorer/README.md

# Cadence AST Explorer

Install the dependencies:

  ```sh
  npm install
  ```

Start the UI:

  ```sh
  npm run start
  ```

In a separate shell, run the API:

  ```sh
  go run .
  ```
